import 'package:flutter/material.dart';

class PageAI extends StatelessWidget {
  const PageAI({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('This is Page AI'));
  }
}