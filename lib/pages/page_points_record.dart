import 'package:flutter/material.dart';

class PagePointsRecord extends StatelessWidget {
  const PagePointsRecord({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('This is Page Points Record'));
  }
}